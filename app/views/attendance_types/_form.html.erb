<%= form_for(@attendance_type, html: { class: 'form-horizontal' }) do |f| %>
  <% if @attendance_type.errors.any? %>
    <div id="error_explanation">
      <h2>
		<%= pluralize(@attendance_type.errors.count, "error") %>
		prohibited this attendance_type from being saved:
	  </h2>
      <ul>
		<% @attendance_type.errors.full_messages.each do |message| %>
          <li><%= message %></li>
		<% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
	<div class="col-md-2"><%= f.label "name" %></div>
	<div class="col-md-10"><%= f.text_field :name %></div>
  </div>
  <div class="actions"><%= f.submit %></div>
<% end %>
